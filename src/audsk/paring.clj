(ns audsk.pairing
  (use [clojure.set :only [map-invert]]))



;; # Device-pairing

;; Given two IDs and an environment,
;; "pair" them such that either ID
;; can access the other.

(defn pair [id1 id2 env]
  (assoc env id-1 id-2))

(defn get-pair )



;;;; 5 FILES
;; - iclaim
;; - ialpa
;; - 
;; - 


